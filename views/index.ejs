<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Monday's chores: </h1>
    <ul class="todoItems">
    <% for(let i=0; i < items.length; i++) {%>
        <% if(items[i].day === "monday") {%>
        <li class="item">
                <% if(items[i].completed === true) {%>
                    <span class='completed'><%= items[i].thing %></span>
                <% }else{ %>
                    <span><%= items[i].thing %></span>
                <% } %>   
                <span class='fa fa-trash'></span> 
                <% } %>
        </li>
    <% } %>
    </ul>

    <h4>Left to do:<%= leftMon %> </h4>

    <h1>Tuesday's chores: </h1>
    <ul class="todoItems">

    <% for(let i=0; i < items.length; i++) {%>
        <% if(items[i].day === "tuesday") {%>
        <li class="item">
            <% if(items[i].completed === true) {%>
                <span class='completed'><%= items[i].thing %></span>
            <% }else{ %>
                <span><%= items[i].thing %></span>
            <% } %>   
            <span class='fa fa-trash'></span> 
            <% } %>
        </li>
    <% } %>
    </ul>

    <h4>Left to do:<%= leftTues %> </h4>

    <h1>Wednesday's chores: </h1>
    <ul class="todoItems">

    <% for(let i=0; i < items.length; i++) {%>
        <% if(items[i].day === "wednesday") {%>
        <li class="item">
            <% if(items[i].completed === true) {%>
                <span class='completed'><%= items[i].thing %></span>
            <% }else{ %>
                <span><%= items[i].thing %></span>
            <% } %>   
            <span class='fa fa-trash'></span> 
            <% } %>
        </li>
    <% } %>
    </ul>

    <h4>Left to do:<%= leftWed %> </h4>

    <h1>Thursday's chores: </h1>
    <ul class="todoItems">

    <% for(let i=0; i < items.length; i++) {%>
        <% if(items[i].day === "thursday") {%>
        <li class="item">
            <% if(items[i].completed === true) {%>
                <span class='completed'><%= items[i].thing %></span>
            <% }else{ %>
                <span><%= items[i].thing %></span>
            <% } %>   
            <span class='fa fa-trash'></span> 
            <% } %>
        </li>
    <% } %>
    </ul>

    <h4>Left to do:<%= leftThurs %> </h4>

    <h1>Friday's chores: </h1>
    <ul class="todoItems">

    <% for(let i=0; i < items.length; i++) {%>
        <% if(items[i].day === "friday") {%>
        <li class="item">
            <% if(items[i].completed === true) {%>
                <span class='completed'><%= items[i].thing %></span>
            <% }else{ %>
                <span><%= items[i].thing %></span>
            <% } %>   
            <span class='fa fa-trash'></span> 
            <% } %>
        </li>
    <% } %>
    </ul>

    <h4>Left to do:<%= leftFri %> </h4>

    <h2>Add A Todo:</h2>
    <form action="/addTodo" method="POST">
        <input type="text" placeholder="Thing To Do" name="todoItem">
        <input type="text" placeholder="Day" name="day">
        <input type="submit">
    </form>
    
    <script src='js/main.js'></script>
</body>
</html>
